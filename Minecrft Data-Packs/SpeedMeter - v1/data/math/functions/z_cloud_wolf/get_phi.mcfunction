#############################################################
# Grabs Phi (rx) and returns it on Phi scoreboard:
# 
# Phi is conventionally the verticle angle from the origin
#
#############################################################

## Store's Entity's Verticle Rotation on Phi ##
execute store result score @s phi run data get entity @s Rotation[1]